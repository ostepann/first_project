"""
Конфигурация для бэктеста и оптимизации Dual Momentum стратегии.
Все настройки хранятся здесь — изменяйте только этот файл.
"""

# === ПАРАМЕТРЫ ОПТИМИЗАЦИИ ===
param_grid = {
    'base_lookback': [10, 20, 50, 75, 100, 126],
    'base_vol_window': [15, 20, 25, 30, 35],
    'max_vol_threshold': [0.3, 0.35, 0.4]
}

# === РЕКОМЕНДОВАННЫЕ ПАРАМЕТРЫ ДЛЯ ТОРГОВЛИ ===
production_params = {
    'base_lookback': 100,
    'base_vol_window': 15,
    'max_vol_threshold': 0.4
}

# === УСЛОВИЯ ТОРГОВЛИ ===

# Комиссия брокера по инструментам (в %)
# Пример: 0.03 = 0.03% за сделку
commission = {
    'EQMX': 0.04,   # ETF на МосБиржу
    'OBLG': 0.03,   # ETF на ОФЗ
    'GOLD': 0.05,   # ETF на золото
    'LQDT': 0.02,   # Денежный ETF (кэш)
}
default_commission = 0.0  # используется, если тикер не указан в словаре

# Проскальзывание по инструментам (в базисных пунктах, bps)
# Пример: 7.5 bps = 0.075%
# Значения основаны на реальных данных MOEX (см. moex_spreads_log.csv)
slippage = {
    'EQMX': 7.5,
    'OBLG': 7.5,
    'GOLD': 10.0,
    'LQDT': 0.6,
}
use_slippage = True  # включить учёт проскальзывания

initial_capital = 100_000  # начальный капитал в рублях

# === ФИЛЬТР ПО ВРЕМЕНИ ===
# Установите '12:00:00', если данные содержат время
# Или None, если данные дневные
trade_time_filter = None

# === ТИКЕРЫ ===
tickers = ['GOLD', 'EQMX', 'OBLG', 'LQDT']
risk_free_ticker = 'LQDT'  # актив без риска (кэш)
market_ticker = 'EQMX'     # для рыночной волатильности и тренда

# === ДИРЕКТОРИИ ===
data_dir = "data"  # относительный путь от корня проекта